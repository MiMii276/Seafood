APP_ENV=prod
APP_DEBUG=0

# DATABASE_URL should be set by Railway via environment variable
# But have a fallback if needed (MySQL):
# DATABASE_URL=mysql://user:pass@host:port/dbname?serverVersion=8.0

# Configure your app URL
DEFAULT_URI=https://seafood-production.up.railway.app

# Disable debug toolbar in production
PROFILER_LISTENERS=false
